# Claude Code instructions

- This repository contains Japanese course materials for building a C subset compiler targeting RISC-V RV64.
- Do not commit, stage, or push without explicit maintainer permission.
- Do not modify, remove, or revert unrelated existing changes.
- Do not write student information, lesson logs, names, hostnames, IP addresses, or other environment-specific identifiers to this repository.
- Keep completed answers, hidden tests, and quality records only in the private `../c-comp-design/` repository.
- Treat `materials/` as source material and `workbook/` as learner-facing distribution. Review learner paths from `workbook/` alone unless instructed otherwise.
- Do not normally inspect generated or dependency directories such as `node_modules/`, `_build/`, `.site/`, `.pages/`, `dist/`, and `__pycache__/`.
- Do not read generated SVG bodies unless investigating rendering or generation differences; inspect their TikZ sources and build steps first.
- Read `AGENTS.md` only when detailed repository navigation, document relationships, or build and test commands are needed.
- Read only the documents and topic directories relevant to the requested task.
