% F4: 宣言か文か — 型キーワード 4 語だけで分岐する
\documentclass[border=8pt]{standalone}
\input{figure-preamble}
\begin{document}
\begin{tikzpicture}[node distance=0mm,
    q/.style={cfgbox, minimum width=52mm, minimum height=8mm},
    code/.style={font=\ttfamily\small, anchor=west, inner sep=1.5pt}]
  % ================ 左: トップレベルの判定 ================
  \node[gannot] at (2mm, 13mm) {parse\_program（トップレベル）};

  \node[q] (t1) at (0mm, 2mm) {struct + タグ + \texttt{\{} か \texttt{;} ?};
  \draw[ptr] (t1.east) -- +(4mm,0);
  \node[annot, anchor=west] at (32mm, 2mm) {struct 定義 / 前方宣言\\（AST には残さない）};

  \node[q] (t2) at (0mm, -14mm) {型を読み，名前の次が \texttt{(} ?};
  \draw[ptr] (t2.east) -- +(4mm,0);
  \node[annot, anchor=west] at (32mm, -14mm) {関数宣言 / 定義};

  \node[q] (t3) at (0mm, -28mm) {それ以外};
  \draw[ptr] (t3.east) -- +(4mm,0);
  \node[annot, anchor=west] at (32mm, -28mm) {グローバル変数宣言};

  \draw[flow] (t1.south) -- node[annot, right=1mm, pos=0.4] {\small いいえ} (t2.north);
  \draw[flow] (t2.south) -- (t3.north);

  % ================ 右: 関数本体の先頭の判定 ================
  \node[gannot] at (95mm, 13mm) {parse\_func\_body（関数本体の先頭）};

  \node[q] (b1) at (93mm, 2mm) {int / char / void / struct ?};
  \draw[ptr] (b1.east) -- +(4mm,0);
  \node[annot, anchor=west] at (125mm, 2mm) {宣言として読む};

  \node[q] (b2) at (93mm, -14mm) {それ以外};
  \draw[ptr] (b2.east) -- +(4mm,0);
  \node[annot, anchor=west] at (125mm, -14mm) {文として読む（parse\_stmt）};

  \draw[flow] (b1.south) -- node[annot, right=1mm, pos=0.4] {\small いいえ} (b2.north);

  \node[code] (c1) at (72mm, -26mm) {is\_type\_start():};
  \node[code] (c2) at (76mm, -32mm) {kind == TK\_KW and sval in};
  \node[code, text=TcEmph] (c3) at (76mm, -38mm) {('int','char','void','struct')};

  % ---- 補足 ----
  \node[gannot, anchor=north, align=left] at (62mm, -46mm)
      {型を書き始められるトークンは 4 語だけで，解析中に増えない。\\
       だから型名の表も，字句解析器へのフィードバック（lexer hack）も要らない};
\end{tikzpicture}
\end{document}
